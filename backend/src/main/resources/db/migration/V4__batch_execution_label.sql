-- Метка массового запуска задачи "Выдача прав доступа..." сразу по всем банкам
-- (см. ExecutionService.executeBatch): одно выполнение фактически прогоняет
-- несколько разных скриптов (по одному на банк, сгруппированных по загруженному
-- Excel-файлу "Счёт/Банк"), поэтому единственный script_id — лишь техническая
-- ссылка (первый обработанный банк), а список реально обработанных банков
-- хранится здесь для отображения в истории.
ALTER TABLE script_executions ADD COLUMN batch_label VARCHAR(1000);
